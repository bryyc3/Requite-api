import { db } from "../db/dbConfig.js";
import { user } from "../db/barrel.js";

export async function testDbInput(){
    // try{
    // //     const test = await db.insert(user).values({
    // //         email: "test",
    // //         reward_tracker: 1,
    // //     });
    // //     return test;
    // // } catch(err){
    // //     console.log(err)
    // }
};